<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorPrimaryBackground</item>
        <item name="android:textColor">@color/textWhitePrimary</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <!-- enable window content transitions -->

        <!-- specify enter and exit transitions -->
        <item name="android:windowAnimationStyle">@style/CustomActivityAnimation</item>

    </style>

    <style name="CustomActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_inn_hoyre</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_ut_venstre</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_inn_venstre</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_ut_hoyre</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="AlertDialogCustom" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Used for the buttons -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">@color/textWhitePrimary</item>
        <!-- Used for the background -->
        <item name="android:background">@color/colorCardBackground</item>
    </style>

</resources>
